#include <iostream>

int main()
{
    // std::cout << "Please enter a number ans press ENTER: ";
    // int x = 0;
    // std::cin >> x;
    // std::cout << "You entered: " << x << '\n';
    // The std::cin is the standard input stream, and it uses the >>
    // operator to extract what has been read into our variable.
    // We can accept several values from the standar input by 
    // separating them with multiple >> operators:

    // std::cout << "Please enter two numbers separated by a space and press ENTER: ";
    // int x = 0;
    // int y = 0;
    // std::cin >> x >> y;
    // std::cout << "You entered: " << x << " and " << y << '\n';

    // We can accept values of different types
    // std::cout << "Please enter a character, an integer and a double: ";
    // char   a = 0;
    // int    b = 0;
    // double c = 0;
    // std::cin >> a >> b >> c;
    // std::cout << "You entered: " << a << ", " << b << ", and " << c << '\n';
}