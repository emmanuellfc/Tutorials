#include <iostream>

int main()
{
    // Section 7.1: Assignment operator
    // The assignment operator = assgins a value to a variable/object.
    // char mychar = 'a';   // Define a char variable mychar
    // mychar      = 'b';   // Assign a new value to mychar

    // int       x = 123;   // Define an integer variable x
    //           x = 456;   // Assign a new value to x
    // int       y = 789;   // Define an integer variable y
    //           y = x;     // Assign the value of x to y
    
    // std::cout << "mychar = " << mychar <<'\n';
    // std::cout << "x = " << x <<'\n';
    // std::cout << "y = " << y <<'\n';

    // Section 7.2: Arthmetic operators
    // We can do arithmetic operations using aritmetic operators.
    // + addition
    // - subtraction
    // * multiplication
    // / division
    // % modulo
    // int x = 123;
    // int y = 456;
    // int z = x + y; // Addition
    // z = x - y;     // Subtraction
    // z = x * y;     // Multiplication
    // z = x / y;     // Division
    // std::cout << "The value of z is " << z << '\n';

    // New try but now with double type
    // double x = 123;
    // double y = 456;
    // double z = x + y; // Addition
    // z = x - y;     // Subtraction
    // z = x * y;     // Multiplication
    // z = x / y;     // Division
    // std::cout << "The value of z is " << z << '\n';

    // Section 7.3: Compund assignment operators.
    // Compound assignment operators allow us to perform and arithmetic 
    // operation and assign the result wiht one operator.
    // += compound addition
    // -= compound subtraction
    // *= compound multiplication
    // /= compound division
    // %= compound modulo
    
    // Example
    
    // int x  = 123;
    //     x += 10; // the same as x = x + 10
    //     x -= 10; // the same as x = x - 10
    //     x *= 2;  // the same as x = x * 2
    //     x /= 3;  // the same as x = x / 3
    // std::cout << "The value of x is " << x << '\n';

    // Section 7.4: Increment and decrement operators
    // Increment and decrement operators allow us to increment or decrement
    // the value of an object.
    // ++x pre-increment operator
    // x++ post-increment operator
    // --x pre-decrement operator
    // x-- post-decrement operator

    // Example
    // int x = 123;
    // x++; // post-increment
    // ++x; // pre-increment
    // --x; // pre-decrement
    // x--; // post-decrement
    // std::cout << "The value of x is " << x << '\n';

    // The difference between pre and post increment/decrement operators
    // is that the pre operators increment/decrement the value of the object by one
    // before the expression is evaluated, while the post operators increment/decrement
    // the value of the object by one after the expression is evaluated.



}