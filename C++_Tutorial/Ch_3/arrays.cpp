#include <iostream>

int main()
{
    // char arr[5]; // array of 5 characters
    // int  arr[5]; // array of 5 integers

    // Now we are going to initialize an array
    int arr[5] = {10, 20, 30, 40, 50};
    // Now access the inidivual elements of the array and change them
    arr[0] = 100; // this will change the first element of the array
}